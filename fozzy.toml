[package]
name = "superctx"
version = "0.1.0"

[[target.bin]]
name = "superctx"
path = "src/main.fzy"

[ffi]
panic_boundary = "error"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
